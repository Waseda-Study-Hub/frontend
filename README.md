
  # Build from provided prompt

  This is a code bundle for Build from provided prompt. The original project is available at https://www.figma.com/design/IiL4ueNDid6kAtW6pOUVh9/Build-from-provided-prompt.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  